{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo building!"
    },
  "deploy": {
     "preDeployCommand": ["sudo apt install nodejs npm"],
    "startCommand": "npm run dev",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "never"
    }
}

